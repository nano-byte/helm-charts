# InitContainer test

name: test
fullname: my-test

image:
  repository: jwilder/whoami
  tag: latest

initContainerTemplates:
  - |
      name: init2
      image: hello-world:{{ .Values.image.tag }}
